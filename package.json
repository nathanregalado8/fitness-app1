{
  "name": "fitness-app1",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Training log with per-muscle tiered progress, recovery warnings, and a two-layer AI coach (Claude via backend proxy).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.5",
    "vite": "^8.2.0"
  }
}
